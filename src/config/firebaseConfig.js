// ============================================================
// LMS Platform — Firebase Configuration (Canonical Location)
// Re-exports from config/ for the new src/ based import paths
// ============================================================

export { auth, db, storage } from '../../config/firebase-config.js';
export { default as app } from '../../config/firebase-config.js';
